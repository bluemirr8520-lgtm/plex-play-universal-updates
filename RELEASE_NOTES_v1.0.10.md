# Plex Play Universal 1.0.10

## Android TV·OTT 리모컨 설정 개선

- 재생 설정을 열 때 뒤 재생 컨트롤의 포커스를 해제하고 설정창의 첫 조작 항목으로
  자동 이동하도록 수정했습니다.
- 선택된 설정 행, 버튼, 자막 옵션 칩과 슬라이더에 최대 4dp 고대비 테두리·배경·굵은 글자
  하이라이트를 적용했습니다.
- 자막 가로·세로 위치 슬라이더에서 좌우 키는 위치 값만 조절하고, 위아래 키는 인접
  항목으로 이동하도록 분리했습니다.
- 공용 슬라이더 탐색을 수정해 고급 화면 설정에서도 위아래 이동이 막히지 않습니다.
- 리모컨 뒤로 버튼과 화면의 뒤로 버튼을 같은 단계 이동 로직으로 통일했습니다.
  `자막 사용자 설정 → 자막 → 재생 설정 메인 → 재생 화면` 순서로 돌아갑니다.

## 유지 사항

- 1.0.9의 평면 2D 아이콘과 TV 배너를 유지합니다.
- 1.0.8~1.0.9의 외부·내장 텍스트 자막 사용자 설정, 범용 재생 안정화와 기존 설정을
  그대로 유지합니다.
